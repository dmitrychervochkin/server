{
  "name": "my-json-server",
  "version": "1.0.0",
  "scripts": {
    "start": "npx json-server --watch db.json --port $PORT --host 0.0.0.0 --middlewares ./cors.js"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
